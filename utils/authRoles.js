// we are creating enums,
// when we want to change some value we only need to change here
// when we want to add something we just need to ad here

const AuthRoles = {
  ADMIN: "ADMIN",
  MODERATOR: "MODERATOR",
  USER: "USER",
};

export default AuthRoles;
